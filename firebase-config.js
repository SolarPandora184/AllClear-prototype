/* =========================================================================
   FIREBASE CONFIG - paste your project's config values below.
   This is the ONLY file you need to edit to turn on Firebase features
   (cached map snapshot + account login). index.html references this file
   and never needs to change.

   Where to get these values: Firebase console -> Project settings ->
   General tab -> "Your apps" -> the web app -> SDK setup and configuration.
   ========================================================================= */
const FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  databaseURL: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
